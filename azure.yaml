# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: WebQWorker
services:  
  app:
    language: dotnet
    project: .\WebQWorker.AppHost\WebQWorker.AppHost.csproj
    host: containerapp
